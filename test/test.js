var assert = require('assert');


describe('Array', () => {

    describe('#indexOf()', () => {

        it('should return -1 when the value is not present', () => {
            throw new Error(`This failed though`)
        });
    });
});
